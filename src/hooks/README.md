# src/hooks

This folder contains reusable React hooks shared across the application.

Guidelines:

- Keep hooks small and single-purpose.
- Avoid side effects outside React lifecycle hooks.
- Prefer generic hooks over page-specific hooks.
