# src/modules

This folder contains feature-level modules composed of sections and UI blocks (e.g., home page sections).

Guidelines:

- Keep modules focused on a single page or feature area.
- Prefer composing from components in src/components and src/common/components.
- Co-locate module-specific styles or helpers within the module folder.
